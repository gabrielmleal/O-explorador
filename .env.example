# Explorador Web App Configuration

# Server Configuration
PORT=3000
NODE_ENV=development

# JWT Configuration (will be used in Task 2)
JWT_SECRET=your-super-secret-jwt-key-here
JWT_EXPIRES_IN=24h

# Database Configuration (if needed for future tasks)
DB_CONNECTION=sqlite
DB_PATH=./webapp/database/app.db

# Security
BCRYPT_ROUNDS=10